  <div class="container mt-4">
    <h1>Daily Live</h1>
    <h2>Ce qu'on vous propose <%= image_tag "quarantine-star.png" %></h2>
      <div class="row mt-5">
        <% @categories.each do |category| %>
          <div class= "col-12 col-md-6 col-lg-3">
              <div class="card-category">
                <%= category.name %>
                <%= link_to "", category_events_path(category.slug) %>
              </div>
          </div>
        <% end %>
      </div>
  </div>
